{% extends 'base.html.twig' %} {% block title %}Hello CategoryController !{%
endblock %} {% block body %}
<div class="col-6 mx-auto">
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>#</th>
        <th>Nom</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      {% if categories %} {% for category in categories %}
      <tr>
        <td>{{ category.id }}</td>
        <td>
          <a href="">{{ category.name }}</a>
        </td>
      </tr>
      {% endfor %} {% else %}
      <tr>
        <td colspan="2">Pas de catégories pour le moment</td>
      </tr>
      {% endif %}
    </tbody>
  </table>
</div>

{% endblock%}
